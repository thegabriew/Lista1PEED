numeros = input("Digite uma sequência de números separados por vírgula: ").split(",")
soma_impares = 0
for num in numeros:
    if int(num) % 2 == 1:
        soma_impares += int(num)
print("A soma dos números ímpares é:", soma_impares)
